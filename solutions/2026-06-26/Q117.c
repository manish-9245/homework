/*
 * Problem: Create student record system using arrays and strings.
 */

#include <stdio.h>
#include <string.h>
int main() {
  char n[10][50];
  int r[10];
  scanf("%s%d", n[0], &r[0]);
  printf("%s %d", n[0], r[0]);
  return 0;
}
